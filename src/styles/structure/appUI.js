import { dimensions, fontSizes, scalingUtils, fontWeights } from '../index'


// Không thực hiện ghi đè (custom) thì để Object rỗng
export default { 
    common: {},
    basic: {},
    component: {},
    layout: {},
    override: {}
}