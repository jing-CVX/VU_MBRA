import { dimensions, fontSizes, scalingUtils, fontWeights } from '../../index'


// Tất cả các style dành cho từng màn hình riêng lẻ
export default {
    Market__Title: {
        flex: 1,
        alignItems: 'flex-start',
        justifyContent: 'center',
        fontSize: fontSizes.medium
    }
}