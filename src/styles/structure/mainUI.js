// import { dimensions, fontSizes, scalingUtils, fontWeights } from '../index'
import {common, basic, component, layout, override} from './main';

// tất cả css, code UI để ở đây
const mainUI = {
  common: common,
  basic: basic,
  component: component,
  layout: layout,
  override: override,
};

export default mainUI;
