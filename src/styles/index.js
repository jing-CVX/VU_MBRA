import fontSizes from './helper/fontSizes';
import * as dimensions from './helper/dimensions';
import * as scalingUtils from './helper/scalingUtils';
import fontWeights from './helper/fontWeights';
import * as IconSvg from './icon-svg';
import color from './helper/color';

export {dimensions, fontSizes, scalingUtils, fontWeights, IconSvg, color};

/**
 * import { dimensions, fontSizes, fontWeights } from '../../styles';
 * thường một component ta chỉ cần import 3 thông số đó là đủ để tạo style cho all object
 *
 */
